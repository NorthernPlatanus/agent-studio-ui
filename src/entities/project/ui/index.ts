/** Presentational bits for `project`. Store readiness is rendered inline by
 * the settings screen and the project switcher, which need it beside other
 * facts rather than as a badge of its own. */
export {};
