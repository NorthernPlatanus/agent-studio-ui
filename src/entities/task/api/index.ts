/**
 * Query hooks for `task`. Shapes come from `src/shared/api/generated.ts`,
 * requests from `shared/api/client`, keys from `taskKeys` (project-first).
 */

export { taskQuery, tasksQuery, useTask, useTasks } from "./task-queries";
