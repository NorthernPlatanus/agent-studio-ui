/**
 * stage-pipeline: plan → wave → dispatch → candidates → gate → visual gate → review → scene verdict → integrate → finalize, with counts.
 * Composed from entity ui/ + features; no direct `fetch`, no sideways widget imports.
 */
export {};
